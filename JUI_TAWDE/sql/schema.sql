-- Assignment 10: Car Rental & Fleet Booking System
-- Run this script in Supabase SQL Editor.

CREATE TABLE vehicles (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    brand TEXT NOT NULL,
    model TEXT NOT NULL,
    year INT NOT NULL,
    category TEXT NOT NULL CHECK (category IN ('Sedan', 'SUV', 'Luxury', 'Hatchback', 'Electric')),
    daily_rate NUMERIC(10, 2) NOT NULL CHECK (daily_rate > 0),
    fuel_type TEXT NOT NULL,
    seating_capacity INT NOT NULL DEFAULT 5,
    status TEXT NOT NULL DEFAULT 'available' CHECK (status IN ('available', 'rented', 'maintenance')),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE rentals (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id UUID NOT NULL,
    vehicle_id BIGINT REFERENCES vehicles(id) ON DELETE RESTRICT,
    customer_name TEXT NOT NULL,
    customer_email TEXT NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    total_cost NUMERIC(10, 2) NOT NULL,
    status TEXT NOT NULL DEFAULT 'booked' CHECK (status IN ('booked', 'active', 'completed', 'cancelled')),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT valid_date_range CHECK (end_date >= start_date)
);

-- Helpful indexes for booking lookups.
CREATE INDEX idx_rentals_vehicle_dates
ON rentals(vehicle_id, start_date, end_date);

CREATE INDEX idx_rentals_user
ON rentals(user_id);

-- Sample fleet (3 vehicles required by the assignment).
INSERT INTO vehicles (brand, model, year, category, daily_rate, fuel_type, seating_capacity)
VALUES
('Toyota', 'Camry', 2024, 'Sedan', 2500, 'Petrol', 5),
('Hyundai', 'Creta', 2025, 'SUV', 3000, 'Petrol', 5),
('Tesla', 'Model 3', 2024, 'Electric', 4500, 'EV', 5);